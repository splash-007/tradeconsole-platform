/**
 * @cryonfx/shared
 *
 * This package will contain safe contracts shared by the CryonFX frontend and backend.
 *
 * Future contents:
 *   - DTOs (Data Transfer Objects)
 *   - Zod validation schemas
 *   - Role constants
 *   - Permission constants
 *   - Enums
 *   - API contracts / response types
 *
 * Do NOT place secrets or backend-only code here.
 * Do NOT import backend-only Node.js modules here.
 *
 * Current status: Foundation only — no types migrated yet.
 * Types will be migrated progressively once the real API is connected.
 */

export {};
